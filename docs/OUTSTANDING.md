# THOTH — outstanding work (consolidated 2026-09-09)

Everything not-done, in one place. Items move here from PHASES/PORT docs when
deferred, and leave with a date + commit when shipped. Nothing here is forgotten.

## A. Blocked on keys (need a human)

| Item | Key | Free tier? | Unblocks | How to enable |
|---|---|---|---|---|
| AIS vessels layer | AISStream | yes, signup | ships layer (only true gap in transport; probed 2026-09-13: every AIS API needs a key or a feed station — no keyless path) | sign up, hand over key; no collector exists yet — key starts the build, it doesn't finish it |
| OTX enabled path | OTX_API_KEY | yes, signup | pulse intel + its test | free signup at otx.alienvault.com → `backend/.env` (dev) or host env → `backend/restart.sh` → `otx` leaves STALE on `/api/health` |
| Conflict depth | ACLED token | researcher | UCDP/ACLED events | hand over token; no collector exists yet |
| Markets depth | FINNHUB_KEY | yes | [x] earnings collector shipped disabled-honest (batch11/12) | free signup at finnhub.io → same place → restart worker → earnings rows land in `markets` |
| Telegram bot | TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID | yes | [x] push sender + `POST /api/notify` + `notify` command shipped (batch11) | BotFather → `/newbot` → message the bot once → chat id via `api.telegram.org/bot<token>/getUpdates` → same place → test with `notify hello` in cmdbar |

## B. Decisions (taken 2026-09-09, all per recommendation)

1. [x] **Cutover done**: tunnel → app :3000
   (`https://ment-pack-remind-historical.trycloudflare.com`); :4000 tunnel killed.
2. [x] **Terminal retired**: `backend/public/index.html` bannered, kept until
   2026-10-09, then delete.
3. [x] **Reskin**: accepted as-is. Rail crowding stays; higher-value builds first.
4. [x] **Mobile shell**: out, permanently. Responsive web app is the mobile story.

## C. Unbuilt features (all specced in PORT/PHASES docs)

- [x] ThreatClock dial — ironsight SVG in bottom strip, live brief DEFCON + sweep (2026-09-09).
- [x] EntityGraph panel — `e`/GRAPH button, center = selection, ring = co-visible
  entities incl. clusters (`layer ×N`), click flies + selects (2026-09-09).
- [x] MiniMap — floating overview, camera sync + viewport rect + click-to-fly,
  hidden on phone per responsive contract (2026-09-09).
- Market analytics HUD/backtest (needs months of our own history first — earliest Q1 2027; sitrep clock verified growing 2026-09-13, 4 snapshots 09-09→09-13 with an honest 09-12 sandbox gap).
- [x] Satellite COG imagery (2026-09-13): `GET /api/imagery?lon=&lat=` serves the freshest low-cloud (<30%, else freshest) Sentinel-2 L2A scene via earth-search STAC (keyless, verified 4645 scenes/6wk, ±0.01° box — zero-area bboxes 400); OBJECT tab shows SATELLITE block (date · cloud% · thumbnail → full-res COG link, honest-empty, img-error fallback). Tests: `imagery.test.ts` 4/4 + alive live/400 entries.
- Gloomberb company exposure/treemap (needs company dataset — do NOT build blind; identity leg shipped 2026-09-13: `company <name>` cmdbar command → GLEIF LEI records via `/api/osint/company`, e2e-covered).
- Video wall 6→10 verified 24/7 streams (TRT World, Euronews, ABC News Live, AlArabiya added 2026-09-13, all oembed-verified evergreen).
- [x] Free-endpoint hunt (2026-09-13, no keys): Frankfurter FX moved to `api.frankfurter.dev` (live) → `fx` collector feeds `markets` (8 USD pairs, daily); Open-Meteo air-quality (live) → new `airquality` map layer (30 world cities, US-AQI severity); World Bank macro (live) → `macro <ISO2/3>` cmdbar command (GDP/GDP-pc/inflation/pop). Dead ends documented: abuse.ch APIs (MalwareBazaar/ThreatFox) now need auth keys; ReliefWeb v1 decommissioned, v2 needs approved appname (our `relief` collector uses their RSS — unaffected).
- [x] Free-endpoint hunt #2 (2026-09-14, no keys): FEMA OpenFEMA declarations → `disasters` (new `fema` collector, 6h); NOAA CO-OPS tide gauges (8 surge stations, datum=MSL — 400 without it) → `oceans`; MET Norway MetAlerts 2.0 (CAP shape: title/when.interval, no id/headline/sent) → `weather`; IODA blackout alerts → `cyber` (new `ioda` collector, skips normal baseline); RDAP (IANA bootstrap) + HackerTarget dns/reverse (50/day, on-demand only) → `routes-recon.ts` (routes-osint kept under cap). fema/ioda sparse-by-design = NEVER_FROZEN. Rejected: OpenAQ v3 + WAQI (keys now), urlscan search (key for quota), SpaceX API (525, maintenance-only), NASA DONKI DEMO_KEY (shared-quota 503 — on-demand only). Queued: OWID grapher CSVs (annual CO2/energy context for macro/energy).
- [x] Free-endpoint hunt #3 (2026-09-14, static+live, no keys): A: OONI confirmed-censorship (new collector) + SANS infocon → `cyber`. B: live ISS fix → `satellites`, Spaceflight News API → `news`, Kalshi statecraft markets → `markets` (keyword-filtered; egress-blocked here, honest-STALE). C: `airports` 375→5280 via OurAirports PD (build-airports.ts; scheduled flag unreliable upstream — kept as metadata, not filter) + `datacenters` +387 via PeeringDB (build-datacenters.ts, idempotent, import-safe mains). D: SEC EDGAR + Cloudflare DoH + GitHub search + FAA drone-airspace bbox → recon (doh/github/airspace live; EDGAR egress-403, honest-502) + EMSC → `quakes`. Skipped with reason: Team Cymru (RIPEstat covers), cable landings (official repo 404, NOAA has corridors-not-landings, mirror is CC BY-NC-SA + unclear paths), HAPI (self-minted identifier + params unverified), NWPS water (exact params TBD), OWID/Ember (still queued). Follow-ups from the hunt itself (all fixed): e2e assumed sparse data — readiness now asserts per-layer presence, pinPoint falls across layers, cluster tries top-4 candidates, phone drills toward clusters + waits for tiles; app reload-throttle 45s/layer in page.tsx (every version bump re-clustered the map and dissolved hovered clusters mid-read; 10 new bumping sources made it constant).
- [x] Hover stranding fix (2026-09-14): cross-layer `closeHovers` removed cards without resetting the victim controller's same-feature key → re-hover early-returned with no card (killed the airwx polygon e2e 5×; denser layers raised the hit rate). `steadyHover` flush now re-attaches when its card is gone and the feature isn't pinned (pin-echo check kept first). Proved with throwaway MutationObserver probes (deleted after).
- [x] Free-endpoint hunt #5 (2026-09-15 #2, no keys, all probe-verified): crypto (Binance+Coinbase) + predict (Manifold) + fxdepth (ECB+fxratesapi) + imf → `markets`; solar (Ovation+X-ray+F10.7) → `spacewx`; quakesnz → `quakes`; storms (NHC 3 basins, quiet heartbeats) → `disasters`; radar → `weather`; energyuk → `energy`; rivers → `oceans`; litwatch (trials+PubMed+HN) → `research`. OSINT +4 (mitre-cve/geocode/macro-imf/ports incl. Shodan internetdb). Dead ends: tsunami.gov refused, exchangerate.host keyed, auroraslive/evp.se down, EIA keyed, SEC companyfacts rate-gated, INEGI HTML-only, restcountries 301.
- [x] Loop tick 31 = batch56 (2026-09-16, no keys, all probe-verified): transit+Oval/High-St-Ken (8 boards) + Fribourg corridor LS-FR/FR-BE (7 legs); energy-eu+HU/SI (EU19, HU 429-transient). batch36 17/17 + batch37 28/28 + batch38 5/5 EXIT=0, tsc+biome clean. Live: transit 242. Test-fix: Fribourg stub was missing (origin=Fribourg, not Lausanne). Dead ends: RO 404, Swiss Cottage/Waterloo 404, Tooting empty.
- [x] Loop tick 32 = batch57 (2026-09-16, no keys, all probe-verified): transit+Waterloo/London-Bridge (10 boards, Ealing guess 404-logged); oceans+coops-wind (5/8 met-equipped, sparse-green pattern) + coops-pressure (8/8 hPa). batch36-40 54/54 EXIT=0, tsc+biome clean. Live: transit 376/hr, oceans 202/hr; health 270/278. Dead ends: UK/SK energy 500-no-content (same family as RO), iRail stations/vehicle empty (skip), NWS atom-zone works-but-redundant (skip), Ealing Broadway arrivals 404-logged.
- [x] Loop tick 33 = batch58 (2026-09-17, no keys, all probe-verified): transit+6 boards (Stratford 9/Canning Town 9/Liverpool St 9/Ealing Bdwy 5/Holborn 9/Bond St 9 — 16 stations total) + roads A1/A10/A13/A40 (18 rows) + 6-line status (piccadilly/northern/bakerloo join, 6 rows) + swiss return legs BE-ZH/GE-BE (6 rows); energy-eu+8 (RO/SK/HR/IE/LU/EE/LV/LT — 25-country loop, 429-transient flips per run, retry-green); oceans+7 CO-OPS ring (Providence/Springmaid/Pilots-Station/San-Diego/Astoria/Seattle/Nawiliwili — coops 7 new live, wind 5/7 + pressure 6/7 met-equipped, sparse-green). energyeu 11/11 EXIT=0 (new EU27 RO/SK/HR suite) + transit-tfl/ch/oceans 22/22 EXIT=0, tsc clean, biome formatted 2 files (1 pre-existing unused-param in oceans). Live --once: transit 324, energyeu 50, oceans 195. Dead ends: energy GB 400 + MT/CY/RS/TR/IS 429 (skip), CO-OPS 9759114 400 (skip). Correction: Ealing Broadway works — batch57's 404-logged was wrong (EBY live, 5 rows).
- [x] Loop tick 34 = batch59 (2026-09-17, no keys, all probe-verified): fx+EUR-base cross (EUR/USD/GBP/JPY/CHF reverse view → `markets`); energy-uk+forecast peak (48h fw window → `energy`) + generation mix (top-3 fuels, wind 56.9% sample → `energy`). fx 3/3 + energyuk 3/3 + sentiment 2/2 (8/8 with retest 6/6), tsc clean, biome clean. Live --once: fx 11 (7 USD + 4 EUR), energyuk 5 (1 now + 1 fw + 3 mix). Parked with reason: energy-charts total_power (fatter dup of public_power), price + signal (real but need spike/threshold rules, not blind rows), dayahead_price 404.
- [x] Loop tick 35 = batch60 (2026-09-17, no keys, all probe-verified): airwx+G-AIRMET area forecasts (29 lines, FZLVL sample) + intl SIGMETs (132 FIR polygons, Mexico TS EMBD sample) → `airwx`; radar+satellite IR leg (same payload `satellite.infrared` key, honest-green at 0 frames) → `weather`; quakesnz+GeoNet volcano VAL (12 NZ volcanoes, Taupo Green) + news feed (1290 total, eruption-keyword watch) → `quakes`; fires+NOAA-21 (95k rows) + Suomi-NPP (83k) rungs (MODIS-C61 404, C6 kept). airwx/radar/quakesnz/fires 7/7 (retest 7/7), tsc+biome clean (removed dead stub helper, template-literal fix). Live --once: airwx 140 (11 + 29 + 100), radar 1 (+IR green), quakesnz 117 (100 + 12 + 5), fires 500. Dead ends: AWC cwa 204-empty + pirep 400-shape (skip), GeoNet titles/intensity/landslides 400-paths (skip), RainViewer IR 0-frames-right-now (honest-green, not a dead end).
- [x] Flowsint digest (2026-09-17): cloned `reconurge/flowsint` to `/workspace/.tmp/flowsint` (14M shallow); audited all enricher upstream hosts, probed keyless candidates live. **Shipped 3 routes**: `sirene` (French SIREN registry, renault → 982770919 live), `stealers` (HudsonRock free tier, unknown → 200 empty), `gravatar` (HEAD d=404 existence + profile). CmdBar + routes-osint test + alive wired; npm test 248/248; backend + app tsc/biome clean. Already-covered (skipped): ip-api, crt.sh. Keyed (skipped): HIBP, Dehashed, Etherscan, Whoxy, c99, WhoisXML-history, Veriphone, PDCP-asnmap. Local-compute (out of scope): subfinder/dnsx/httpx/naabu, sherlock, reconcrawl, tech-detect. Receipt: `docs/PORT-flowsint.md`; ENDPOINTS OSINT 57→60, route table 82→85.
- [x] Fincept digest batch61 (2026-09-17, no keys, all probe-verified): markets+CBOE US (VIX 15.44/SPX/NDX/RUT — no ^DJI in feed, RUT covers) + CBOE-EU flagships (UK100/DAX40/CAC40/IBEX35/FTSEMIB/ES50) + FiscalData avg-rates (Bills 3.788/Notes/Bonds/TIPS) → `markets`; imf+DBnomics BEA GDP ($30.76T 2025 via mirror, no BEA key) → `markets`; research+Yahoo symbol search (AAPL/MSFT/NVDA/TSLA + sector/industry — security-master seed) → `research`. markets/imf/research 16/16 (retest 16/16), tsc+biome clean. Live --once: markets 92, imf 11, research 126. Dead ends: CBOE VIX futures 403-S3, OECD SDMX dataflow 404 (their own scripts admit drift), WTO 401-subscription, Congress API_KEY_MISSING, UNESCO data needs geoUnitType (fixed), WITS 307-HTML.
- [x] Fincept digest batch62 (2026-09-17, no keys, all probe-verified): health+UNESCO enrolment (indicator 20062, USA 24.5M 2016, 25/25 watchlist countries live → `health`); symbol static via build-symdir.ts (nasdaqtrader SymDir 12,516 symbols, blank-flag fix — D/E/H are test variants, N + blank live) seeded to static/symdir.json + `GET /api/osint/symbol?q=` prefix search (AAPL/NVDA live). health 4/4 EXIT=0, tsc+biome clean. Dead ends: none — both first-try green after probe.
- [x] Keyless expansion batch65 (2026-09-18, no keys, all probe-verified): imf+World Bank WDI source-series (govt-debt %GDP USA 115.8/GBR 130.7 2024 + real GDP growth USA 2.2/CHN 5.0 2025, null-skipping latest-year, debt>100 watch) → `markets` (worldbank-src); markets+CoinGecko movers board (top-3 price+chg+rank) + trending names (top-4 by mcap rank) → `markets` (cg-movers/cg-trending); crypto+Coinbase spot BTC/USD+EUR last-trade + Llama Aave/Lido TVL spot-checks → `markets` (coinbase-spot/llama-tvl). imf/markets/crypto 19/19 (retest 19/19), tsc+biome clean. Live --once: imf 15 (11 + 4 WB-src), markets 99, crypto 24. Dead ends: ransomware.live 429 (1/min quota — parked, needs throttle slot), SWPC mag-1day/proton-flux 404 (path drift), Llama /tvl lowercase-solana empty + overview/stablecoins/volumes/fees 404-500 (dump + /tvl/{aave,lido} + coins endpoint carry), FRED/UNRATE via DBnomics 404 (wrong dataset mirror path), GDELT doc API 429 (needs 5s throttle — parked), Etherscan V1 deprecated + Ethplorer freekey invalid-address (skip).
- [x] Fincept digest batch63 (2026-09-18, no keys, all probe-verified): hdx+CKAN catalog pulse (Canada 576 gdp / Swiss 1321 energy / AU 11679 climate / Slovenia 3 energy — package_search counts → `disasters`). hdx 3/3 EXIT=0 (retest 3/3), tsc+biome clean. Live --once hdx 51 (47 IDMC + 4 CKAN). Dead ends: US catalog.data.gov 404 (fincept URL stale — re-add when confirmed), Italy dati.gov.it 403, Brazil dados.gov.br 401, Uruguay untested.
- [x] Terminal pillar batch64/fincept-UI (2026-09-18): 006_terminal migration (portfolios/positions/notes/screens, idempotent) + 8 routes in routes-intel (CRUD + 400s, zod, upsert ids) + PULSE/PORTFOLIO/SCREEN/NOTES tabs (TerminalTabs.tsx, fincept shapes: MarketPulsePanel sections, WatchlistScreen table, ScreenerScreen sorts, FinancialNote model) + symbol route/tests/alive + CmdBar unaffected (object-tab osint path). npm test 255/255, app tsc/biome/vitest 15/15 clean, backend tsc/biome clean. Live-verified CRUD + cleanup (probe rows deleted). Single-operator scope (no users/roles). Receipt: docs/PORT-fincept.md; route table 85→86. Deliberately deferred: live quote tables (licensed), brokers, SSO/RBAC, MCP/agents, node workflows, backtesting (needs history), ontology + DataHub fan-out (design passes).
- [x] Terminal inspection fixes (2026-09-18, self-inspected in Chromium via Playwright): bitfinex index fix (LAST_PRICE r[7], rel-change r[6] — was $0.002/+430% spoofing SCREEN gainers; live now ETH $2,453.9/BTC $76,612) + FX id namespacing (fxecb:/fxnbp: — shared fx:USDXXX collided across sources, ON CONFLICT kept stale source on retitled rows e.g. frankfurter wearing NBP title) + PULSE FX pick-list completion (ecb/nbp/fxrates/erapi-usd/erapi-eur/frankfurter-eur surfaced) + PULSE EQUITIES quotes-first ordering (yahoo before nasdaq-top/moex under slice cap) + TerminalTabs exhaustive-deps/format cleanup. fxdepth/fx 7/7, app tsc/biome/vitest clean, backend tsc/biome clean, browser re-verified. fxdepth test needed thoth_test migrate (relation events missing after fresh pg install). + pulse-cmdbar deep links round-2 (2026-09-18, second Chromium pass): ER-API RFC2822 date fix (slice gave 'Fri, 18 Se' in ids/titles → parsed to ISO 2026-09-18, live now USD/GBP 1.3371) + EUR-leg USD/USD identity-row skip (degenerate eurUsd/eurUsd=1, EUR/USD covered by ecb/frankfurter/yahoo) + Yahoo prev-close fallback (previousClose null live → chartPreviousClose then regularMarketChangePercent-derived; NVDA +3.38% not +0.00%) + fiscal-rates title de-dup ('US Treasury Treasury Bills' → 'US Bills/TIPS'; nasdaq-top meta +chg for SCREEN sort) + CmdBar pulse|portfolio|screen|notes → onTab deep links + help line + render test. fxdepth/markets 17/17, app vitest 16/16, browser re-verified (all four checks true, zero page errors), demo rows deleted via API. + phone UI refine (2026-09-18, measured 390x844 in Chromium): active tab scrolls into view on deep link (notes sat at x=561 in a 361px strip — now scrollIntoView on tab change) + sheet grabber affordance (::before 40x4 pill) + terminal section heads (dim caps, first head keeps title voice) + rows/selects/cmdbar at 44px tap height + overflow fix (.cmdbar negative margins pushed body to 392px — now padding-only, back to 390) + toasts pinned to sheet. breakpoints 4/4, vitest 16/16, tsc+biome clean, zero page errors. + airwx overlap refine (2026-09-18, Globe screenshots): 476 stacked fills at 0.28 composited into dark blobs (no antimeridian giants — max span <60d) → zoom-scaled wash (0.05/0.08/0.14) + severity outlines (critical solid red 2px / watch amber 5,3-dash / info thin dotted) x airwx+perims, setVis extended to -o-crit/watch/info, hover/pick path untouched (fill layer id unchanged) — hud polygon test green. Icons round-2 (Lucide ISC, via jsdelivr): conflicts→swords, oceans→ship, ports→anchor, spacewx→sun, signals→radio-tower, metar→plane-takeoff, forecast→thermometer, drones→quadcopter, datacenters→server-racks, cctv→security-camera; catalog 3/3 + vitest 16/16 clean, zero page errors. + server-monitor pass (2026-09-18): feeds block removed from Explorer (production surface = layers only) + MONITOR inspector tab (MonitorTab.tsx: 56 collector groups, rows with state · healthy · last update · period every X · missed ×N, filter + ISSUES toggle, ungrouped fallback) + /api/health enriched with collector/intervalSec via source-map.ts (317/317 live sources mapped, frankfurter→fx canon) + health pill + SERVER MONITOR link + monitor|health|feeds cmdbar → tab opens + floating Panel button deleted (☰ sheet is the nav pane: tap layers directly) + Inspector tab-scroll rAF fix (scrollIntoView no-ops on display:none sheet). backend 255/255 + collectors 17/17, app 17/17, alive 3/3, breakpoints 4/4 (rail test re-cut to pill-open), hud status-strip re-cut to monitor rows, tsc+biome clean, zero page errors.
- [x] Test refactor per-collector/per-route (2026-09-17, user rule: no subagents — all work direct): 40 batch files → 49 collectors-*.test.ts (transit ×3 + collectors-lib; vatsim/hans/autobahn suites written fresh, batch21 originals lost pre-port) + 4 routes-*.test.ts (core/intel/osint/recon); batch35 + endpoints.test.ts deleted. Full collectors 166/167 (forecast/marine pre-existing — fails on batch19 alone too); npm test 242/242 (LIVE_LAYERS trimmed of static catalogs absent from stats; brief counts ≥L-3; routes accept honest 400/502; alive threshold 33→27). Cross-suite contamination fixed 8× (shared-table scoping: oceans datum, energyeu DE, weather metalerts/metsun/ns, transit-ch LS legs, quakes emsc, satellites tle-mirror/amsat, flights fallback/garbage/bosporus). Keyed suites (otx/finnhub) use runtime config keys, disabled-last order. Lost with deleted files: batch16 iss-live/snapi/kalshi depth, batch18 parseTelegramPosts/ecdc/epa-ie, batch21 vatsim/hans/autobahn/ghsa/nbp/depths (rewritten fresh where noted), batch22 ingv/neo/ukbills/rocketlive/ivao/secrss/rivers-flood, batch23 adsbfi/stack, batch24 swiss-rail/brightsky/owid/sunspots/registry, batch30 geofon/luftdaten, batch31 510k/mbta/defi; unit.test.ts still covers hans/parseUnXml parsers.
- [x] Loop tick 30 = batch55 (2026-09-16, no keys, all probe-verified): flights+opensky-tokyo/sydney/mexico (70+5+13 live — Tokyo Bay ANA/JAL, Sydney QantasLink, Mexico AeroMexico). batch39 2/2 EXIT=0, tsc+biome clean. Live: flights 336+. Dead ends: Kyiv bbox null-states (closed airspace — excluded by design), HR/SK 404-no-content, IE works-future, opensky-history 403.
- [x] Loop tick 29 = batch54 (2026-09-16, no keys, all probe-verified): flights+opensky-bosporus (35 live, Istanbul bbox — independent poll appended to EU tick, own ids/health). batch39 1/1 EXIT=0, tsc clean. Live: flights opensky 321+35. Dead ends: adsb.lol still 503 (opensky carries), opensky-history 403 (no poll leg), airplanes.live 403-contact.
- [x] Loop tick 28 = batch53 (2026-09-16, no keys, all probe-verified): transit+Paddington/Victoria (6 boards) + Lausanne-Bern 5th leg; energy-eu+HU/SI (EU19, HU 429-transient). batch36 17/17 + batch37 28/28 + batch38 3/3 EXIT=0, tsc+biome clean. Live: transit 230, energyeu 38+. Dead ends: RO 404-no-content (skip), Swiss Cottage/Waterloo 404, Tooting empty.
- [x] Loop tick 27 = batch52 (2026-09-16, no keys, all probe-verified): transit+Green Park arrivals (4 boards) + Bluebikes/Toronto (GBFS×5); weather+sunsched Berlin/Istanbul (×4); energy-eu+FI/NO/CZ (EU17). batch36 17/17 + batch37 28/28 EXIT=0, tsc+biome clean. Live: transit 205+, energyeu 38+. Dead ends: Swiss Cottage/Waterloo 404 (Green Park works), Tooting empty (skip), CH hourly-slots (skip).
- [x] Loop tick 26 = batch51 (2026-09-16, no keys, all probe-verified): research+lunar/debris topics + markets Yahoo AAPL/NVDA/silver/natgas + nasdaq-top screener (NVDA $5.16T leads). batch36 17/17 + batch37 26/26 EXIT=0, tsc+biome clean. Live: research 87, markets 78. Bug-fix: nasdaq empty-cap rows (Number('')===0 passes filter — require cap>0+finite px). Dead ends: Nasdaq needs browser UA (works), SpaceX 525, DONKI-demo 429, Mars-photos 404-app, APOD-demo 429.
- [x] Loop tick 25 = batch50 (2026-09-16, no keys, all probe-verified): transit+Bluebikes Boston + BikeShare Toronto (GBFS×5); weather+sunsched Berlin/Istanbul (×4). batch36 16/16 + batch37 25/25 EXIT=0, tsc+biome clean. Live: transit 205, weather 400. Dead ends: Toronto PBSC shape differs (no capacity — handled optional), PHIVOLCS/DMI dead, DONKI-demo 429, energy/power-alias 500.
- [x] Loop tick 24 = batch49 (2026-09-16, no keys, all probe-verified): quakes-asia+JMA Yokohama/Sapporo (6-city loop, title-case fix); rivers+Tone-Tokyo/Yodo-Osaka gauges; forecast+Sapporo city (33). batch36 16/16 + batch37 23/23 EXIT=0, tsc+biome clean. Live: quakesasia 88, forecast 63, rivers 22. Dead ends: OM ICON/best_match models novelty (skip), Ishikari-flood novelty (skip).
- [x] Loop tick 23 = batch48 (2026-09-16, no keys, all probe-verified): transit+tfl-status (3 line-health rows) + irail-conn (Brussels→Antwerp journeys); quakes-asia+JMA Nagoya/Fukuoka (4-city loop); oceans+coops-pred (4 hilo predictions). batch36 16/16 + batch37 22/22 EXIT=0, tsc+biome clean. Live: transit 186, quakesasia 80, oceans 165. Test-fix: nagoya stubs passed 500 as body (ok() default is 200 — 500 means res.ok false). Dead ends: JMA area-code guessing (230000/400000 verified-live), FDA-food 000 (parked), iRail connections needs -L (stealthFetch follows).
- [x] Loop tick 22 = batch47 (2026-09-16, no keys, all probe-verified): energy-eu+PT/GR (EU14); transit+tfl-arr-eus/Euston + swiss LS-GE return leg; OSINT maltiverse extended to IPv4 (+asn/city). batch36 16/16 + batch37 19/19 EXIT=0, tsc+biome clean. Live: energyeu 38, transit 187. Maltiverse 429-transient (route honest-502, retry-ok). Dead ends: CH hourly-slots (skip), installed-power yearly (skip), Victoria line-status empty, Mode/status 404.
- [x] Loop tick 21 = batch46 (2026-09-16, no keys, all probe-verified): energy-eu+SE/DK (EU12); transit+tfl-arr-eus/Euston + swiss GE-LS third leg. batch36 16/16 + batch37 17/17 EXIT=0, tsc+biome clean. Live: energyeu 32, transit 176. DK 429-transient (retry-ok pattern, throttle holds). Dead ends: CH hourly-slots (skip), installed-power yearly (skip), ElectricityMap 401, Line/victoria empty, Mode/status 404, Journey-meta novelty, OV-fiets 404.
- [x] Loop tick 20 = batch45 (2026-09-16, no keys, all probe-verified): energy-eu+BE/AT (EU10 complete); transit+tfl-arr-kx (King's Cross board) + swiss BE-GE leg (two-route loop); test-infra: batch36 split 31→16+15 (batch37 file, both EXIT=0). batch36 16/16 + batch37 15/15, tsc+biome clean. Live: energyeu 29, transit 173. Dead ends: line-status Victoria empty (skip), Mode/status 404, OV-fiets GBFS 404, BE Load/Residual rows are aggregates (top-3 positive filter keeps Solar/Wind).
- [x] Loop tick 19 = batch44 (2026-09-16, no keys, all probe-verified): energy-eu+NL/PL (EU8 complete — 1.5s throttle beats 429); quakes-asia+JMA-Osaka (313 rain-watch); transit+Divvy Chicago + CaBi DC (GBFS loop ×3, per-system health). batch36-file 29/29 EXIT=0, tsc+biome clean. Live: energyeu 20, quakesasia 74, transit 157. Test-fix: retired stale Tokyo-only suite (superseded by Tokyo+Osaka). Dead ends: OV-fiets GBFS 404, NL 429-without-throttle (fixed), KX arrivals work (future leg).
- [x] Loop tick 18 = batch43 (2026-09-16, no keys, all probe-verified): quakes-asia+turkey-kandilli/afad (13+13 live, geojson, mag>=2.0); crypto+deribit-funding (BTC-PERP 8h rate); OSINT +maltsearch (Maltiverse IOC search, live-verified emotet). batch36-file 26/26 EXIT=0, tsc+biome clean both sides. Live: quakesasia 69, crypto 20. Dead ends: AFAD-direct 302 (mirror works), BMKG-alt dead, IRIS 404, Deribit options-book 400KB-fat (skip), ElectricityMap 401, Nordpool 401, ECDC 404, OTX auth, FullHunt dead, Greensnow 503.
- [x] Loop tick 17 = batch42 (2026-09-16, no keys, all probe-verified): crypto+ETH index + deribit-dvol split (own source/budget); cyber+blocklistde (25 German-community IPs); energy-eu+ES/IT mixes (EU4 complete: DE/FR/ES/IT). batch36-file 24/24 EXIT=0, tsc+biome clean. Live: crypto 18, cyber 384, energyeu 13. Test-fix: retired stale BTC-only deribit suite (superseded by ETH+DVOL). Dead ends: Greensnow 503, OTX auth-required (403), Maltiverse search works-keyless (future: search route), ransomware groups 1/min rate-limit (victims is the poll leg), cyberattacks path 302-docs.
- [x] Loop tick 16 = batch41 (2026-09-16, no keys, all probe-verified): crypto+deribit (BTC index + DVOL); litwatch+themuse (8 jobs) + openfood (Nutella sentinel) + musicbrainz (Nevermind 258, 503-flaky retried-ok); OSINT +crfunder (Crossref funders, live) + food (OFF search, live) + music (MB search, 503-now honest-502) — all alive+CmdBar+api+OsintView wired. batch36-file 21/21 EXIT=0, tsc+biome clean both sides. Live: crypto 17, litwatch 102. Dead ends: IRIS 404 (NGF page), French fuel dataset-id guessed 404, Nordpool 401, ECDC atlas 404, MusicBrainz 503-busy (retried-ok), BA CKAN 10s-latency, OTX shape keyed-ish, FullHunt dead, XForce/ThreatMiner keyed/dead.
- [x] Loop tick 15 = batch40 (2026-09-16, no keys, all probe-verified): transit+tfl-arr (Baker St live arrivals, countdown leg); energy-eu+energy-charts-fr (FR top-3 mix); OSINT +maltiverse (hostname reputation, live) + urlscan (domain scan history, live) — both alive+CmdBar+api+OsintView wired, 400-validated. batch36-file 19/19 EXIT=0, tsc+biome clean both sides. Live: transit 158, energyeu 11. Dead ends: DONKI needs key (403), BA CKAN 10s-latency (skip), UK floods empty-now (skip), urlscan api-v1 301→404 (v1 path is /api/v1/search POST-less GET works), FullHunt dead, XForce 401, OTX shape keyed-ish, DB APIs dead/503, ResRobot/SL keyed, Entur/Digitransit keyed.
- [x] Loop tick 14 = batch39 (2026-09-16, no keys, all probe-verified): cyber+ransomware (20 victims, RaaS leg) + msrc (Patch-Tuesday releases); markets+pypi-dl (hugovk ClickHouse top-10 monthly) + rubygems (rails totals) + jsdelivr (express versions). batch36-file 17/17 EXIT=0, tsc+biome clean both sides. Live: cyber 359, markets 69 (pypi 10). Dead ends: pypistats 429 both paths, pepy.tech 401-keyed, ECB XML unreachable, urlscan POST-less 301 (needs POST), OTX 200-but-keyed-shape, XForce 401, ThreatMiner dead, thum.io live-but-binary (skip).
- [x] Loop tick 13 = batch38 (2026-09-16, no keys, all probe-verified): cyber+threatfox (25 IOCs, conf>=50) + bazaar (20 sample hashes, quoted-CSV) + dshield-top (10 scanner IPs, tab-pairs) + cins (25 hostile IPs, pipe-list); imf+worldbank (USA/CHN/IND/DEU/JPN nominal GDP); quakes-asia+jma-forecast (Tokyo 3-day wx codes, rain-watch). batch36-file 15/15 EXIT=0, tsc+biome clean both sides. Live: cyber 336, imf 10, quakesasia 45. Dead ends: ECB XML unreachable, Statnett empty, JMA-tokyo is forecast not quake (labeled), urlhaus 478KB-fat polled not stored-raw-heavy (cap 50), CINS is reputation not conviction (conf 0.75).
- [x] Loop tick 12 = batch37 (2026-09-16, no keys, all probe-verified): weather+sunsched (Kyiv/Warsaw daylight via sunrise-sunset.org, theater-town leg); transit+tfl-bike (Santander 800 docks, idx%53 sampler) + tfl-road (A2/A3/A4 disruptions, point-coord parse) + swiss-conn (Zurich→Bern journeys); research topics 3→5 (+critical minerals, +subsea cables); forecast cities 24→32 (+Taipei/Manila/Hanoi/Riyadh/Nairobi/Lima/Bogota/Anchorage); OSINT +omgeo (OM geocoding forward search, alive+CmdBar+api+OsintView wired, live 200). batch36 12/12, batch35+unit 42/42, batch34+unit 35/35, tsc+biome clean both sides. Live: transit 152, forecast 62, weather 409, research 102. Test-infra fix: batch35 18-suite hang fixed by splitting into batch35(9)+batch36(9) files + order-independent irail assert + 54-row BikePoint mock. Dead ends: DB transport.rest 503, ResRobot/SL need keys, Entur/Digitransit-NL keyed/401, opensky-Kyiv null (no traffic), opensky-history 403, adsb-fi host dead (opendata.adsb.fi works), airplanes.live 403-contact, Stooq 404, SSB 410, NWIS 503, OM customer-API keyed, OM ERA5 unreachable, SSemScholar 429, Unpaywall 422, Barcelona CKAN accident CSVs yearly-static (skip).
- [x] Loop tick 11 = batch36 (2026-09-16, no keys, all probe-verified): imf+boc-fx (BoC Valet CAD×4); markets+nbp-pln (PLN×4) + cbr (RUB×3, windows-1251 XML, comma-decimals÷Nominal); transit+irail (Brussels liveboard 12) + digitraffic (Helsinki gzip rail 11); litwatch+arbeitnow (10) + remoteok (10 salary); weather+metocean-ns (North/Norwegian Sea); oceans+coops-temp (3 water-temp, SF/Boston have no temp sensor — Atlantic City does). Tests: 13/13 live-path suites green individually (full-file run hangs post-suite-13 on unreaped worker conn — runner infra, not collector code; direct collect() traces all DONE). Live: oceans 161, imf 5, markets 57, transit 127, litwatch 92, weather 407. Dead ends: Geneva RKI ArcGIS 400, HSL realtime dead, data.gov.fi dead, Trafikverket needs POST+key, Aviation-Edge keyed, Stooq CSV 404, SSB dataset 410, Hubeau 403, RWS 404, UK flood timeout, Windy keyed, ER-API v6 keyed (open/ free works), OpenCitations v1 404, AEMET keyed, DMI path guessed, MET sunrise/textforecast v2 EOL'd, novelty APIs live-but-novelty (parked).
- [x] Loop tick 10 = batch35 (2026-09-16, no keys, all probe-verified): cyber+feodo (5 C2, quoted-CSV) + dshield (20 top /24s, TSV — DShield format is tabs not spaces); social+lobsters (15) + devto-security (10); airquality+sg-psi (5 regions); forecast batch34 pair covered; fxdepth+frankfurter (USD×4 ECB-ref); energy-eu+energy-charts (DE top-3 mix); policy+govtrack (5 bills, flaky-500 retried-ok — UA-independent); transit+septa (15 buses) + septa-rail (15 trains); litwatch+pubmed-latest (esearch→esummary, 3 newest papers); weather+yr-nowcast (Oslo/Bergen/Trondheim precip). batch35 12/12, batch34+35+unit 47/47, tsc+biome clean. Live: cyber 256, social 106+, airquality 44, forecast 51, fxdepth 49, policy 65, transit 105, litwatch 72, weather 392. Dead ends: SSLBL CSV deprecated 2025-01-03 zero rows (dropped pre-ship); SpaceX v4 525; open-notify astros stale-2024; Hubeau 403; OpenCitations v1 404 (needs new API); AEMET needs key; DMI path guessed; MET Norway bare-lat 400 (needs lon too).
- [x] Loop tick 9 = batch34 (2026-09-16, no keys, all probe-verified): forecast+ipma (Lisbon/Porto/Faro day-0, in-row lat/lon) + iss-now (open-notify second live ISS fix); social+radio (15 geo community-radio stations, ungeocoded skipped). batch34 2/2, batch33+34+unit 36/36, tsc+biome clean. Live --once forecast 51, social 75, all 3 feeds ok. Dead ends: SpaceX v4 525 (skip), open-notify astros crew names stale-2024 (iss-now is the live one), radio-browser topvote ungeocoded (search+has_geo_info works).
- [x] Loop tick 8 = batch33 (2026-09-16, no keys, all probe-verified): civic+sf311 (15 live rows, geo, data.sf.gov — sfgov.org 301-redirects). batch33 1/1, batch32+batch33+unit 37/37, tsc+biome clean. Live --once civic 70. Dead ends: Seattle Socrata id guessed (404), Denver redirect (socrata dead, arcgis hub now), Baltimore (arcgis hub), Boston (CKAN not Socrata), Philly (403 CloudFront), Chicago taxi id guessed (404), SF fire-incident id guessed (404), Socrata discovery API (404 wrong domain).
- [x] Loop tick 7 = batch32 (2026-09-16, no keys, all probe-verified): satellites+amsat-tle (29 ham TLEs propagated) + amsat-status (19 Heard reports geocoded via Maidenhead); maidenheadToLatLon() exported + unit-tested (JN77sn/FN21 centers, garbage rejected). batch32 3/3, batch31+batch32+unit 39/39, tsc+biome clean. Live --once satellites 99. Dead ends: r4uab satonline 54KB-TLE OK but SatNOGS-TLE-shape dup (skip), mmccants zip (needs unzip path, skip), TLE-mirror only covers 8 curated IDs.
- [x] Loop tick 6 = batch31 (2026-09-16, no keys, all probe-verified): health+fda-510k/ndc (123); transit+mbta (74); markets+defi/cg-exchanges (50). batch31 3/3, batch30+unit 38/38. All 5 feeds ok. Dead ends: PhishTank (302-gated), SCEDC (space-delimited, skip), SMHI station-set (period-link maze, skip), GeoNames demo (dead), MBTA needed %-encoding (works).
- [x] Loop tick 5 = batch30 (2026-09-16, no keys, all probe-verified): quakes+geofon (135); fxdepth+bitfinex/kucoin (44); airquality+luftdaten (40); markets+npm-dl/crates-trend + social+gh-events (43/65). OSINT 57→60 (dailymed/holidays/npm-dl). batch30 5/5, batch29+unit 35/35. kucoin flaked once post-restart (200 on retest — transient). Dead ends: OKX/Bybit/Gemini (bot-block), Blockstream (unreachable), restcountries (deprecated), Copernicus EMS (404), TWSE 1MB (too fat, skip).
- [x] Loop tick 4 = batch29 (2026-09-16, no keys): metar wdir-VRB-string fix (live --once 80, awc-metar green — health 7→6); markets+npm-dl/crates-trend (43); social+gh-events (65). batch29 2/2, batch28+unit 36/36. All 3 feeds ok. Dead ends: restcountries v3 deprecated, Copernicus EMS 404, SEC EDGAR 403 (bot-block).
- [x] Loop tick 3 = batch28 (2026-09-16, no keys): metar obsTime epoch-number fix (live --once 40) + hans vnum-null fix (live --once 69) — health failing 8→7 (hans green); health+fda-faers (121); civic+austintraffic (55); research+openaire-OAF (82). batch28 3/3, unit+batch22 48/48. All 3 feeds ok. Dead ends: VAERS (unreachable), exchangerate.host (keyed), Airlabs (demo dead).
- [x] Loop tick 2 = batch27 (2026-09-16, no keys, all probe-verified): research+core (title/year keys)/figshare (verified shape); fxdepth+paprika 3 coins (live --once 42); cyber+spamdrop 60 nets (live --once 191). batch27 3/3, batch26+unit 37/37. All 4 feeds ok. Dead ends: OpenAIRE nested-OAF (unparseable, skip), exchangerate.host (keyed).
- [x] Loop tick 1 = batch26 (2026-09-16, no keys, all probe-verified): weather+yr/nws-obs/fmi/dwd-warn (live --once 372); solar+silso-daily (4); transit+gbfs/tfl-aq/tfl-tube (61); markets+moex-IMOEX-2361 (37). batch26 4/4, batch25+unit 39/39. All 9 feeds ok. Dead ends: SMHI pmp3g path 404, Kyoto Dst 404, USGS geomag 404, RaspberryShake unreachable, CBOE 403, FMI needs exact storedquery (works).
- [x] Free-endpoint hunt #10 (2026-09-16 batch25, no keys, all probe-verified): relief+ocha/ifrc (live --once 40); crypto+gateio/spamrep (15); litwatch+medrxiv date-windowed (69); disasters+sentry Palermo-sorted top-10 (76); forecast+BOM short-geohash 4 cities (47). OSINT 51→57 (rxnorm/chembl-.json/sbdb/deps-POST-fallback-GET/nasa-img/planespotter-contact-UA). batch25 6/6, batch24+25 11/11. All 6 routes 200, all 7 feeds ok. Dead ends: ChemBL bare path is XML (needs .json), PatentsView GET/POST both gated, BOM long-geohash 400s (short works), planespotters bare-UA 403s (contact UA works).
- [x] Free-endpoint hunt #9 (2026-09-16 batch24, no keys, all probe-verified): 55→56 collectors (+transit: swiss exact-local-names München/Wien not HB-suffixed; delay≥15 watch, ≥3 delayed disruption row); forecast+brightsky/nasa-power (-999 fill skip); statics owid-energy 59 (EU aggregate dropped, OM-geocode centroids) + sunspots 120 (SIFSO Brussels tag) seeded live (energy 1618+59, spacewx +120); OSINT 49→51 (funder/museum). batch24 5/5, batch23+24 12/12. New layer `transit` 33 rows live, catalog+intel mission. Dead ends: FRED graph CSV (conn dies, needs key), FAO v1 unreachable, GovInfo 401, OpenCorporates 401, Aleph 404, CORS proxies all dead, DB transport 503, OBB/DBF HTML, BrightSky is the DWD win.
- [x] Free-endpoint hunt #8 (2026-09-16 batch23, no keys, all probe-verified): 54→55 collectors (+social: masto 3 tags/reddit ArcticShift/lemmy/flickr/inat/gbif → news); research+zenodo/hal/inspire; fxdepth+erapi-usd/eur (WANT cross math) + coinlore; imf+bls-cpi (MoM); weather+metocean/metsun (sunrise date=dynamic-today); flights+adsbfi-mil fallback; litwatch+stack (createHash import added). OSINT 40→49 (fda-drug/gene/ontology/protein/package/daylight/zip/transit/name). batch23 7/7, batch22+23 22/22, unit 33/33, tsc+biome clean both sides. Live --once: social 56, research 50, fxdepth 39, imf 1, weather 326, flights opensky 159, litwatch 69. Dead ends: Bluesky searchPosts 403, Google Books 429, planespotters UA-gated, EIA QB HTML, BLS flat 403.
- [x] Free-endpoint hunt #7 (2026-09-16 batch22, no keys, all probe-verified): 51→54 collectors (quakes-asia JMA+BMKG, civic NYC311/Chicago/LA, energy-eu DK-spot+carbon-hist); quakes+INGV (pipe col 10, not 11); weather+metnow/nws-fx/HKO; spacewx+scales/WWV/GOES-xray-1d (DONKI DEMO_KEY 429-for-hours — swapped pre-ship); disasters+JPL-CAD (NEO DEMO_KEY 429-for-hours — swapped pre-ship, H<22≈large, AU→LD×389.17); markets+global/blockchair/gold/NYFed/Fiscal; cyber+5 sec-RSS; health+FDA recalls; news+WHO-RSS; policy+UK bills (no SortOrder param); vatsim+IVAO; sats+rocketlive; rivers+OM-flood. OSINT 31→40 (robtex/fdic/doh-cf/token/wikidata/wiki/books/stack/nominatim). batch22 tests 15/15, batch21 regression 10/10, tsc+biome clean both sides, alive entries added. Dead ends: EIA QB HTML (not JSON), BLS flat 403, Census 302, Swiss/DB transit (503/keyed), N2YO keyed, SpaceX 525, CelesTrak timeout-from-here, VAERS/WONDER/CMS dead, game/trivia live-but-novelty (parked), MET-NO ocean/sunrise/AQ probed-live parked for batch23.
- [x] Free-endpoint hunt #6 (2026-09-15 #3, no keys, all probe-verified): vatsim → `flights`, hans → `volcanoes` live, autobahn → `disasters`; kraken/bitstamp/mempool → crypto.ts, nbp → fxdepth.ts, ghsa → cyber.ts, spacedevs/satnogs → satellites.ts, doaj/datacite → litwatch.ts; UN XML 1,011 rows via build-unsanctions.ts; OSINT +2 (ghsa/ror). Batch21 tests 10/10, unit 33/33. Stack rebuilt natively post-box-reset (apt postgres + tsx + cloudflared, no docker). Dead ends: PredictIt CF-challenge, ThreatMiner timeout, PhishTank challenge, Metaculus auth, UNHCR 404, SPC 404, CISA news.xml 403, Overpass runtime error, Census empty, restcountries v3 deprecated, satnogs stations/observations 404 (transmitters works).
- Telegram bot push (needs token from A).
- [x] Free-endpoint hunt #4 (2026-09-15, no keys): Batch A — AWC METAR+TAF 44 stations → `metar`, Open-Meteo forecast+marine 24 cities → `forecast`, Fear & Greed → `markets` (fng never frozen). Batch B — EPSS/OSV/CIRCL vuln routes + Google-DoH + ipwhois recon (5 new OSINT routes, CmdBar + OsintView + epss e2e). Batch C — OpenAlex+Crossref+EuropePMC+arXiv → `research`, WHO GHO 6×25 → `health` (weekly, never frozen), IDMC/HDX 25 countries → `disasters` (never frozen), Federal Register → `policy`. Statics — WRI v1.3.0 (idempotent +0), NE ports 52→1104, UCDP GED v26.1 10→70 (streaming single-pass aggregate, downloads path only). Tests: unit 33/33 + batch19/20/21 files (needs thoth_test) + alive 33 layers + smoke epss step. Docs: ENDPOINTS/DATA_SOURCES/PHASES receipts. Dead ends: NGA WPI 503 (NE instead), UNECE LOCODE 403, ResourceWatch 403 (direct WRI zip), GIDD XLSX-only (per-country CSVs), arXiv 429 (backoff-honest), GED 274MB (stream, not load).
- [x] Status strip consolidation (2026-09-10): ticker pill now
  ZULU · DEFCON · LIVE/STALE/FROZEN · entities total · Kp (spacewx, colored
  5+/7+); extras desk-only, rails keep the compact pill. Tab rail dead controls
  (find/theater) hidden; map attribution dark + compact.
- [x] HUD redesign (2026-09-11): verbose hover cards (severity/source/age/coords
  + cluster summaries via clusterProperties) on points, clusters and polygons;
  dots 0.72→0.5 (critical 1.0→0.66), clusters smaller (r10-25, radius 45);
  unified map-level click picker (same-layer AND cross-layer stacks, deduped,
  cluster-zoom guard) replacing load-order-luck selection; selection auto-reveals
  the inspector on tab/phone/focus (incl. phone display fix); rounded system
  (--r-sm/md/lg/pill) + spacing rhythm + focus rings + scrollbars; feeds show
  relative ages/errors, rows show refresh cadence, object view shows AGE + full
  meta/body via ObjectDetail, SSE connection state in pill tooltip with resync
  on reconnect. e2e: hud 14 + breakpoints 4 (18 new, 28 app e2e total),
  screenshots kept in app/e2e/shots/.

## D. Ops hardening (production-grade remainder)

- [x] Static seed in prod path (Dockerfile COPY static/ + compose `seed` service, 2026-09-09;
  image build itself still unverified — sandbox denies mounts).
- [x] Backup/restore runbook `docs/BACKUP.md` — daily pg_dump cron, restore verify,
  dump vs re-seed recovery paths (2026-09-09; drill itself needs a real host).
- Permanent host deploy (tunnel is temporary by nature).
- `docker compose up --build` never verified here (sandbox denies mounts) — first
  green build must happen on a real host.
- Flights quota: OpenSky 100/day anon — 15-min cadence holds, but a key raises it.
- [x] Monitoring (ROADMAP P1, 2026-09-23): run/source/upstream-call history,
  worker heartbeat + schedule, feed alerts (failing / frozen / mass) with
  Telegram push, run-now queue, `/metrics`, batched retention, Monitor v2
  data tables. Alert delivery to a real Telegram chat unverified here.
- [x] Production hardening pass (2026-09-23, see PRODUCTION.md): Express 5 (async
  errors no longer hang requests), JSON 404/500 + request ids + access log,
  security headers, CORS that honours CORS_ORIGIN, per-client rate limiting via
  TRUST_PROXY (was one shared bucket behind the Next proxy), API_WRITE_KEY write
  gate (fail closed in prod) + app-side injection behind APP_BASIC_AUTH,
  livez/readyz, shared SSE poller + resume replay, graceful API/worker shutdown,
  tracked transactional migrations, pool error handling + statement timeout,
  per-batch layer_versions bumps, maplibre-gl 6 (critical XSS advisory).
- [x] Deploy fixes (2026-09-23): compose DB volume pointed at the wrong PGDATA
  (data lived in the container layer — dump before upgrading, PRODUCTION.md §5);
  app image build failed on a missing public/ dir; browser bypassed the app to
  hit :4000; API port no longer published on 0.0.0.0; log rotation.
- [x] CI deterministic (2026-09-23): fixture dataset + statics seeded, jobs split
  (static / db / app / docker image build), Node 22, dependabot. Upstream-bound
  checks left: alive warm-up (attempts only), RIPEstat/NVD/crt.sh lookups.
- [x] UI redesign (2026-09-23): warm-black/bone tokens, faience single accent,
  severity-only data colour, monochrome glyphs + per-severity map sprites,
  neutral clusters with severity rings, square segmented controls, Plex
  type, grouped layer list, DEFCON meter (replaced the dial), severity-edge
  brief, collapsible feed gaps, phone map-first layout, tablet sheet anchored
  under the status bar. Fixed on the way: timeline never drew (read b.n,
  API serves count), phone sheet inherited tablet rail cells.
- [x] Modern UI pass (2026-09-23): full-bleed map with floating glass panels
  (desk/tab/phone), radius + motion token scales, raised segmented cells,
  hover/press/focus-visible states, sheet slide motion, reduced-motion and
  reduced-transparency fallbacks, map camera padding from panel rects
  (globe centres in the free area), measured `--dock-h`. E2e point pickers
  now require the map canvas to be the hit target. vitest 37/37, e2e 28/29
  locally (ASN lookup needs network; green in CI).
- [x] Sources batch31 (2026-09-23): navwarn (NGA MSI), gpsjam (ADS-B NACp,
  derived), advisories (US State Dept), SPC storm reports, NTWC/PTWC
  tsunami bulletins, JTWC cyclones. New `pruneStale()` store helper for
  current-picture sources (only after a complete, non-empty poll). Map:
  polygon layers now render point geometries too (`<layer>-p`, pickable).
  Contract tests 212/212 collectors (+21 new), fixtures for the 3 new
  layers, e2e 28/29 locally (ASN needs network).
- [x] Floating UI (2026-09-23): collapsible panels with edge handles and
  clear view (`\` / CLEAR; `/` restores the dock), camera eases into the
  freed space, state remembered; pop-out object windows from pinned cards
  (drag, raise, minimise tray, leader lines, live refresh on SSE, layout
  remembered; tablet/phone = swipeable stack); hover/pin/picker cards
  anchor inside the free area (`fitAnchor`). e2e: new floating.spec.ts
  (5 tests), full suite 33/34 locally (ASN needs network).
- Batch31 live verification: the sandbox that built these has no egress,
  so field names follow the published formats (NGA `broadcast-warn`,
  readsb `nac_p`/`gpsOkBefore`, SPC 3-section CSV, tsunami.gov Atom
  `geo:lat`, JTWC RSS + `web.txt`, State Dept `Title` "X - Level N").
  Run `npm run dev:worker -- --once <name>` for navwarn, gpsjam, spc,
  tsunami, storms, advisories on a networked host and check /api/health.
  The gpsjam NACp<8 threshold may need tuning against gpsjam.org once live.
  UK FCDO advisories deferred: no level in the index, needs ~226
  per-country fetches. Owner: next data pass.
- Basemap recolour: CARTO dark-matter stays as served (cool neutral). A
  warm-tuned style (own style JSON over the same tiles) would finish the
  palette; needs visual verification against live tiles. Owner: next UI pass.
- CSP for the app — needs an audited allowlist (CARTO tiles, Esri imagery,
  Google Fonts, video embeds). Owner: next hardening pass.
- Multi-replica API: rate-limit + SSE client state are in-process; move to
  Postgres/Redis before running >1 API container.
- Per-user identity/audit for writes (single shared key today).

## E. Tests (89 → 128, 2026-09-09)

- [x] Sanctions bulk-loader mock-fetch suite — batch5.
- [x] App e2e for /ops route + new widgets.
- [x] OTX-enabled path — batch6 with dummy key + stubbed fetch.
- [x] React render tests — jsdom + testing-library, 10 tests (primitives,
  ThreatClock, EntityGraph, CmdBar routing). Gap closed, nothing remains.
- [x] Perims/airwx mock suite — batch7 (polygon round-trip, severity maps).
- Flake fixes: collector suites serial (`--test-concurrency=1`, TRUNCATE races),
  CI/e2e `REQUESTS_PER_MIN=2000` (page loads cost ~35 req — all from one IP
  in tests; production limits are per client since 2026-09-23).
- [x] Collector suite 9m → 27s (2026-09-23): pg pool `allowExitOnIdle` (each file
  idled 10s) + `THOTH_DELAY_SCALE=0` for backoff sleeps; forecast/marine stub
  ordering bug fixed (marine rows were never exercised).
- [x] Coverage hermeticity (2026-09-10): `test:coverage` runs on thoth_test and
  excludes the HTTP liveness suite; batch TRUNCATEs wiped the dev DB twice
  before this (repaired via seed-statics + --once cycles). Satellites fallback
  hardened: CelesTrak hang now falls through to tle-mirror.
- [x] Stats-decay fix (2026-09-13): `storeNormalized` + `seed-statics` upserts now
  refresh `ingested_at` on conflict (last-seen-in-a-poll; `ts` untouched so
  content_ts/freeze contracts hold) — previously re-seen rows kept first-seen
  timestamps and aged out of the 24h stats window, failing the live suite 3
  days after any reseed. Covered by batch10 store test; dev refreshed via
  reseed + --once cycles (gdacs/oceans/radiation/conflicts/disasters/cctv/
  satellites/volcanoes). Ops note: backend must run with
  `REQUESTS_PER_MIN=2000` (CI value) or the live suite 429s itself; default
  120/min is for production serving.
- Known flaky upstream: crt.sh 502/429s for hours — endpoint retries once (5s,
  429/502/503 only), tests accept honest errors (never fake names).
- [x] Alive verification (2026-09-09): backend `test/alive.test.ts` + app
  `e2e/alive.spec.ts` run in CI on every push.
- [x] Satellites fallback (2026-09-09): CelesTrak 403s this egress persistently;
  `tle-mirror` source live, batch8 covers it.
- [x] Phone/tab responsive fixes (2026-09-09): ticker overflow, tabs scroll,
  tl-hint hidden on phone, overflow asserted per breakpoint.

## F. Cannot verify in this sandbox (by nature)

- GitHub Actions execution (YAML complete, all steps green locally).
- Docker image builds, tunnel permanence, real-device touch testing.
